# --- macOS Sistem Dosyaları ---
.DS_Store
.AppleDouble
.LSOverride
._*

# --- Xcode / iOS ---
# Derleme ve geçici dosyalar
build/
DerivedData/
*.moved-aside
*.xccheckout
*.xcscmblueprint

# Kullanıcıya özel ayarlar (Bunları repoya atma, senin pencerelerinin konumu vs.)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserdatad/
UserInterfaceState.xcuserstate

# CocoaPods (Eğer kullanıyorsan)
# Pods klasörünü atmak genellikle önerilmez, `pod install` ile herkes kendi kurar.
Pods/
Podfile.lock

# Swift Package Manager
.swiftpm/xcode/
.build/

# --- Güvenlik ve Sertifikalar (ÇOK ÖNEMLİ) ---
# Özel anahtarlar ve sertifikalar asla gitmemeli
*.p12
*.cer
*.pem
*.key
*.mobileprovision
*.provisionprofile

# Apple Music / APNs Anahtarları (Özellikle senin projen için)
AuthKey_*.p8

# --- Backend / Node.js ---
# Bağımlılıklar (Bunlar çok yer kaplar, npm install ile kurulur)
node_modules/
jspm_packages/

# Loglar
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# Çevresel Değişkenler (API Key'lerin burada durur, repoya gitmemeli)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# --- Editörler ---
.vscode/
.idea/